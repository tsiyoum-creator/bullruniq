# OS
.DS_Store

# Node / Netlify
node_modules/
.netlify/
*.log

# Secrets — never commit; these live in Netlify env vars
.env
.env.*
!.env.example
# Local tooling (never deploy — publish = "." serves the repo root)
.claude/
