{
  "name": "bullruniq",
  "version": "1.0.0",
  "private": true,
  "description": "BullrunIQ — AI financial command center (Netlify functions)",
  "scripts": {
    "test": "node tests/auth.test.js && node tests/shared.test.js && node tests/stripe-webhook.test.js && node tests/sync.test.js"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0"
  }
}
